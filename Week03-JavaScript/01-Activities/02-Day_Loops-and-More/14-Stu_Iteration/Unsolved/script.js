// Define array
var students = ["Jack", "Alex", "Chee", "Al", "Casey"];

// Check length of array
console.log(students.length);

// Running a for loop, starting from var 0, for all the variables, to read "Great to see you "
for (var i = 0; i < students.length; i++) { 
    console.log("Great to see you " + students[i] + ".");
}
